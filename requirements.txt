# 核心依赖（与 pyproject.toml 同步维护）；安装：pip install . 或 pip install -r requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
pyyaml>=6.0
markdown>=3.6
pymdown-extensions>=10.0
itsdangerous>=2.1
bcrypt>=4.1
nh3>=0.2
